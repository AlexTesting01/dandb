{
  "devDependencies": {
    "cypress": "^14.5.2",
    "cypress-xpath": "^2.0.1",
    "fs-extra": "^11.3.0",
    "npm-run-all": "^4.1.5",
    "pixelmatch": "^7.1.0",
    "pngjs": "^7.0.0"
  },
  "scripts": {
    "test:customer": "cypress run --spec 'e2e/suites/customer_service_suite.js' --browser chrome --headed",
    "test:cart": "cypress run --spec 'e2e/suites/cart_flow_suite.js' --browser chrome --headed",
    "test:parallel": "run-p test:customer test:cart"
  }
}
